mutation updateCard($cardData: UpdateAspectInput!) {
  updateAspect(aspectData: $cardData) {
    id
    nameID
    profile {
      id
      displayName
      visual(type: BANNER) {
        id
        uri
      }
    }
  }
}
