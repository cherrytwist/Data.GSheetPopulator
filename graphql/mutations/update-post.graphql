mutation updateCard($postData: UpdatePostInput!) {
  updatePost(aspectData: $postData) {
    id
    nameID
    profile {
      id
      displayName
      visual(type: BANNER) {
        id
        uri
      }
    }
  }
}
