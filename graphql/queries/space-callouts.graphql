query spaceCallouts($id: UUID_NAMEID!) {
  hub(ID: $id) {
    id
    nameID
    profile {
      displayName
    }
    collaboration {
      id
      callouts {
        nameID
        id
        type
        posts {
          id
          nameID
        }
      }
    }
  }
}
